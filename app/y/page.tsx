import { redirect } from "next/navigation";

export default function YearbookIntermediateRoute() {
  // TODO: Redirect to 404-like page telling the user to actually select a yearbook.
  redirect("/");
}
